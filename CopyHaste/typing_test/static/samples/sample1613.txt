
if __name__ == '__main__':
	
	filenames = [x for x in os.listdir(LastFM_save_address) if '.csv' in x]
	CoLinReward = {}
	GOBLinReward = {}
	RandomReward = {}
	Uniform_LinUCBReward = {}
	LinUCBReward = {}
	Temp1 = {}
	Temp2 = {}
	CoLinRatio = {}
	Uniform_LinUCBRatio = {}
	LinUCBRatio = {}
	GOBLinRatio = {}
	tim = {}
	GOBtim = {}
	userNum = 100
	for x in filenames:	
		filename = os.path.join(LastFM_save_address, x)
		if 'LastFM_100_shuffled' in x:
			i = -1 
			with open(filename, 'r') as f:
				print str(filename)	  
				for line in f:
				   
					i = i + 1
					words = line.split(',')
					if words[0].strip() != 'data':
						continue
					RandomReward[i], Uniform_LinUCBReward[i], LinUCBReward[i], CoLinReward[i],GOBLinReward[i]= [float(x)