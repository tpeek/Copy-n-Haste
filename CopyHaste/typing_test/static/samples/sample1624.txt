
if __name__ == '__main__':
	
	filenames = [x for x in os.listdir(LastFM_save_address) if '.csv' in x]
	CoLinReward = {}
	GOBLinReward = {}
	RandomReward = {}
	LinUCBReward = {}
	Temp1 = {}
	Temp2 = {}
	CoLinRatio = {}
	LinUCBRatio = {}
	GOBLinRatio = {}
	tim = {}
	GOBtim = {}
	for x in filenames:	
		filename = os.path.join(LastFM_save_address, x)
		if 'LastFM_100_shu' in x:
			i = -1 
			with open(filename, 'r') as f:
				print str(filename)	  
				for line in f:
				   
					i = i + 1
					words = line.split(',')
					if words[0].strip() != 'data':
						continue
					RandomReward[i], CoLinReward[i], LinUCBReward[i], GOBLinReward[i]= [float(x) for x in words[2].split(';')]
					tim[i] = i
	print len(tim), len(GOBLinReward)
	
	plt.plot(tim