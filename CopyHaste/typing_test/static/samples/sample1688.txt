
ProgName = "Bible Book Orders tests"
ProgVersion = '0.85'
ProgNameVersion = "{} v{}".format( ProgName, ProgVersion )
sourceFolder = "."
sys.path.append( sourceFolder )
class BibleBookOrdersConverterTests( unittest.TestCase ):
	def setUp( self ):
		self.bbosc = BibleBookOrdersConverter.BibleBookOrdersConverter().loadSystems()
	def test_1005_str( self ):
		result = str( self.bbosc )
		self.assertTrue( isinstance( result, str ) )
		self.assertTrue( len(result) > 20 )
	def test_1010_len( self ):
		self.assertTrue( 10 < len(self.bbosc) < 50 )
	def test_1020_
		result = self.bbosc.
		self.assertTrue( isinstance( result, tuple ) )
		self.assertEqual( len(result), 2 )
	def test_1030_pickle( self ):
		self.assertEqual( self.bbosc.pickle(), None )
	