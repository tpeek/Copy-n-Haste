
ProgName = "Bible Books Codes tests"
ProgVersion = '0.71'
ProgNameVersion = "{} v{}".format( ProgName, ProgVersion )
sourceFolder = "."
sys.path.append( sourceFolder )
class BibleBooksCodesConverterTests( unittest.TestCase ):
	def setUp( self ):
		self.bbcsc = BibleBooksCodesConverter.BibleBooksCodesConverter().loadAndValidate()
	def test_1010_str( self ):
		result = str( self.bbcsc )
		self.assertTrue( isinstance( result, str ) )
		self.assertGreater( len(result), 20 )
	def test_1020_len( self ):
		self.assertGreater( len(self.bbcsc), 100 )
		self.assertLess( 100 < len(self.bbcsc), 255 )
	def test_1030_
		result = self.bbcsc.
		self.assertTrue( isinstance( result, dict ) )
		self.assertEqual( len(result), 17 )
		for dictName in ("referenc