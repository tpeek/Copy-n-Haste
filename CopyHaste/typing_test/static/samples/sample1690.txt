
ProgName = "Bible Books Names tests"
ProgVersion = '0.31'
ProgNameVersion = "{} v{}".format( ProgName, ProgVersion )
sourceFolder = "."
sys.path.append( sourceFolder )
class BibleBooksNamesConverterTests( unittest.TestCase ):
	def setUp( self ):
		self.bbnsc = BibleBooksNamesConverter.BibleBooksNamesConverter().loadSystems()
	def test_1010_str( self ):
		result = str( self.bbnsc )
		self.assertTrue( isinstance( result, str ) )
		self.assertTrue( len(result) > 20 )
	def test_1020_len( self ):
		self.assertTrue( 3 < len(self.bbnsc) < 20 )
	def test_1030_
		result = self.bbnsc.
		self.assertTrue( isinstance( result, tuple ) )
		self.assertEqual( len(result), 2 )
	def test_1040_pickle( self ):
		self.assertEqual( self.bbnsc.pickle(), None )
	d