
ProgName = "Bible Punctuation Systems tests"
ProgVersion = '0.42'
ProgNameVersion = "{} v{}".format( ProgName, ProgVersion )
sourceFolder = "."
sys.path.append( sourceFolder )
class BiblePunctuationSystemsConverterTests( unittest.TestCase ):
	def setUp( self ):
		self.bpssc = BiblePunctuationSystemsConverter.BiblePunctuationSystemsConverter().loadSystems()
	def test_1005_str( self ):
		result = str( self.bpssc )
		self.assertTrue( isinstance( result, str ) )
		self.assertTrue( len(result) > 20 )
	def test_1010_len( self ):
		self.assertTrue( 2 < len(self.bpssc) < 20 )
	def test_1020_
		result = self.bpssc.
		self.assertTrue( isinstance( result, dict ) )
		self.assertGreater( len(result), 2 )
	def test_1030_pickle( self ):
		self.assertEqua