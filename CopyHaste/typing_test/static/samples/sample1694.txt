
ProgName = "Bible Versification Systems tests"
ProgVersion = '0.48'
ProgNameVersion = "{} v{}".format( ProgName, ProgVersion )
sourceFolder = "."
sys.path.append( sourceFolder )
class BibleVersificationSystemsConverterTests(unittest.TestCase):
	def setUp( self ):
		self.bvssc = BibleVersificationSystemsConverter.BibleVersificationSystemsConverter().loadSystems()
	def test_1010_str( self ):
		result = str( self.bvssc )
		self.assertTrue( isinstance( result, str ) )
		self.assertTrue( len(result) > 20 )
	def test_1020_len( self ):
		self.assertTrue( 10 < len(self.bvssc) < 50 )
	def test_1030_
		result = self.bvssc.
		self.assertTrue( isinstance( result, dict ) )
		self.assertEqual( len(result), len(self.bvssc) )
	def test_1040_pickle( self )