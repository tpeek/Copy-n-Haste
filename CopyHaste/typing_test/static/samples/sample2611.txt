
__author__ = "Jose Fonseca et al"
if sys.version_info[0] >= 3:
	PYTHON_3 = True
	def compat_iteritems(x): return x.items()
	def compat_itervalues(x): return x.values()
	def compat_keys(x): return list(x.keys())
	basestring = str
	unichr = chr
	xrange = range
else:
	PYTHON_3 = False
	def compat_iteritems(x): return x.iteritems()
	def compat_itervalues(x): return x.itervalues()
	def compat_keys(x): return x.keys()
try:
except ImportError:
	pass
MULTIPLICATION_SIGN = unichr(0xd7)
def times(x):
	return "%u%s" % (x, MULTIPLICATION_SIGN)
def percentage(p):
	return "%.02f%%" % (p*100.0,)
def add(a, b):
	return a + b
def fail(a, b):
	assert False
tol = 2 ** -23
def ratio(numerator, denominator):
	try:
		ratio = float(numerator)/float(denominator)
