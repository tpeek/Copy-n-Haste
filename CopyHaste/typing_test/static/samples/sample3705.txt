def publish(args):
  parser = get_args_parser()
  opts = parser.parse_args(args)
  mock_data_enabled = opts.mock
  device_name = opts.device_name
  device_id = opts.device_id
  cloudbrain_address = opts.cloudbrain
  buffer_size = opts.buffer_size
  device_port = opts.device_port
  pipe_name = opts.output
  publisher = opts.publisher
  run(device_name,
   mock_data_enabled,
   device_id,
   cloudbrain_address,
   buffer_size,
   device_port,
   pipe_name,
   publisher)
def subscribe():
  raise NotImplemented("Subscribe is not implemented")
def parse_args():
  parser = argparse.ArgumentParser()
  subparsers = parser.add_subparsers()
  publish_parser = subparsers.add_parser('publish',
					  help="Publish data stream - For example: cloudbrain 